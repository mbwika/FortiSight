node_modules

# Build output — regenerated by `npm run build` (locally or in CI); nothing
# reads the committed copy since deployment now builds fresh in CI.
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
